=link_to t('add_new'), new_maintenance_path(@maintenance), class: 'btn btn-primary'

.autopagerize_page_element
  %table.table.table-hover
    %thead
      %tr
        %th{scope: 'col'}= Maintenance.model_name.human
        %th{scope: 'col'}= Car.model_name.human
        %th{scope: 'col'}= Maintenance.human_attribute_name(:mileage)
        %th{scope: 'col', colspan: '2'}

      = render @maintenances

= paginate @maintenances