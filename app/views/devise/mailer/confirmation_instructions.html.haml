%p "#{t('devise_views.mailer.confirmation_instructions.welcome')} #{@email}!"
%p= t('devise.mailer.confirmation_instructions.confirmation_instruction')
%p= link_to t('devise.mailer.confirmation_instructions.confirm'),
confirmation_url(@resource, confirmation_token: @token)
