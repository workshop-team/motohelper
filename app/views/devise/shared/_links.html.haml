- if controller_name != 'sessions'
  = link_to t('devise.registrations.sign_in'),
  new_session_path(resource_name)
  %br/
- if devise_mapping.registerable? && controller_name != 'registrations'
  = link_to t('devise.registrations.sign_up'),
  new_registration_path(resource_name)
  %br/
- if show_forgot_your_password
  = link_to t('devise.sessions.user.forgot_your_password'),
  new_password_path(resource_name)
  %br/
- if devise_mapping.confirmable? && controller_name != 'confirmations'
  = link_to t('devise.registrations.no_confirmation_instruction'),
  new_confirmation_path(resource_name)
  %br/
- if show_unlock
  = link_to t('devise.registrations.no_unlock_instruction'),
  new_unlock_path(resource_name)
  %br/
- if devise_mapping.omniauthable?
  - resource_class.omniauth_providers.each do |provider|
    = link_to "Sign in with #{OmniAuth::Utils.camelize(provider)}",
    omniauth_authorize_path(resource_name, provider)
    %br/
